## Overview
This project implements an automated color palette extraction system using **K-Means clustering**.  
It identifies dominant colors from an image and generates **complementary, analogous, and triadic** color harmonies.

## Key Features
- Dominant color extraction using K-Means
- Color name mapping using CSS3 color names
- Visualization of color distribution and palettes
- Comparison with Decision Tree–based color detection

## Technologies Used
- Python
- OpenCV
- scikit-learn
- Matplotlib
- NumPy
